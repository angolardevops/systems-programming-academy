[package]
name = "repository"
version = "0.1.0"
edition = "2021"
description = "Repository Pattern & Dependency Injection (Rust) — Part 2 companion."
authors = ["Walter Angolar"]
license = "MIT"

[lib]
path = "src/lib.rs"
